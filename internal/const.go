package internal

import "time"

const (
	Version = "2.6.2"

	Timeout     = 10 * time.Second
	RatingLimit = 5
	MarkdownOpt = "Markdown" // TODO change to v2

	ShareBtn = "share"

	HelpCmd    = "/help"
	HCmd       = "/h"
	StartCmd   = "/start"
	VerCmd     = "/ver"
	VCmd       = "/v"
	TopCmd     = "/top"
	BottomCmd  = "/bottom"
	ProfileCmd = "/profile"
	TopicCmd   = "/topic"
	EventCmd   = "/event"
)
